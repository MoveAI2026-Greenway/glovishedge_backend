# GlovisHEDGE Backend — Claude Code Project Instructions

이 파일은 GlovisHEDGE 백엔드 구현 작업에서 Claude Code가 항상 따라야 하는 프로젝트 공통 지침이다.

본 프로젝트는 기존 팀 명세를 기준으로 구현한다. Claude Code가 기능, API, DB 구조, 계산식, 화면 계약을 임의로 재설계하지 않는다.

---

# 1. 작업 시작 전 반드시 읽을 문서

백엔드 관련 구현·수정·리팩터링·테스트를 시작하기 전에 다음 문서를 확인한다.

## 최우선 구현 가이드

@docs/GlovisHEDGE_BACKEND_DB_IMPLEMENTATION_GUIDE.md

## 현장 재구현 명세

@docs/specs/00_INDEX.md  
@docs/specs/01_구현순서_타임박스.md  
@docs/specs/02_계산명세.md  
@docs/specs/03_API계약.md  
@docs/specs/04_화면명세.md  
@docs/specs/05_프롬프트_전문.md  
@docs/specs/06_함정목록.md  
@docs/specs/07_검증체크리스트.md  
@docs/specs/08_데이터시드.md  
@docs/specs/09_문구집.md  
@docs/specs/10_컴포넌트명세.md  
@docs/specs/11_수용기준.md  
@docs/specs/12_영상제작.md  
@docs/specs/13_발표자료제작.md  
@docs/specs/14_배포_제출.md

백엔드 작업에서는 특히 다음 문서를 우선 확인한다.

1. `GlovisHEDGE_BACKEND_DB_IMPLEMENTATION_GUIDE.md`
2. `00_INDEX.md`
3. `01_구현순서_타임박스.md`
4. `02_계산명세.md`
5. `03_API계약.md`
6. `06_함정목록.md`
7. `07_검증체크리스트.md`
8. `08_데이터시드.md`
9. `10_컴포넌트명세.md`
10. `11_수용기준.md`

---

# 2. 문서 우선순위

문서끼리 내용이 충돌할 경우 임의로 합치거나 새로운 사양을 만들지 않는다.

기본 우선순위는 다음과 같다.

```text
최신 현장 재구현 문서
    ↓
GlovisHEDGE_BACKEND_DB_IMPLEMENTATION_GUIDE.md
    ↓
기존 API/DB 설계 문서
    ↓
구버전 프로토타입 코드 또는 과거 문서
```

단, `GlovisHEDGE_BACKEND_DB_IMPLEMENTATION_GUIDE.md`에서 `[결정 필요]`로 표시된 항목은 확정 설계가 아니다.

해당 항목은 반드시 사용자 또는 팀의 결정을 받은 뒤 구현한다.

---

# 3. 프로젝트 핵심 원칙

다음 원칙은 어떤 작업에서도 변경하지 않는다.

```text
숫자 → 계산 엔진
검색 근거 → 데이터/색인
설명 → LLM
```

LLM이 금액, 확률, 거리, 리드타임 등의 수치를 새로 만들어서는 안 된다.

GlovisHEDGE는 가격 방향을 예측하는 서비스가 아니다.

가격의 방향을 단정하지 않고 다음을 제공한다.

```text
가격 구간
도달 확률
비용 비교
실행 가능성
리스크 관리 판단 근거
```

---

# 4. 절대 임의 변경 금지

사용자의 명시적 지시 또는 팀 결정 없이 다음 작업을 하지 않는다.

- 기존 API 경로 변경
- Request/Response 필드명 변경
- 새로운 API 임의 추가
- 기존 테이블 삭제
- 기존 컬럼 의미 변경
- 계산식 변경
- 계산 상수 변경
- 최신 데이터시드를 구버전 값으로 변경
- 화면 문구 임의 수정
- LLM에 수치 계산 위임
- 실제 해상거리 대신 Haversine 직선거리 사용
- 외부 API 출처 변경
- 목업 데이터를 실제 데이터로 표현
- 최신 명세와 구버전 명세를 임의로 혼합
- `[결정 필요]` 사항을 임의 확정
- 필요하지 않은 프레임워크나 라이브러리 추가
- 구현 범위를 임의 확장
- T1/T2보다 T3/T4 기능을 우선 구현

구조적으로 더 좋아 보인다는 이유만으로 팀 설계를 재설계하지 않는다.

---

# 5. 구현 전 현재 상태 확인

코드를 수정하기 전에 반드시 현재 repository 상태를 먼저 확인한다.

최소 다음을 확인한다.

```text
현재 branch
git status
프로젝트 디렉터리 구조
build.gradle
application.yml / application.properties
db/migration
현재 Entity
현재 Repository
현재 Service
현재 Controller
현재 테스트
```

이미 구현된 코드가 존재하면 새로 만드는 대신 기존 구조를 먼저 분석한다.

동일 기능을 중복 구현하지 않는다.

---

# 6. 작업 시작 시 답변 형식

사용자가 구현을 요청하면 코드 수정 전에 먼저 아래 형식으로 짧게 보고한다.

```text
[작업 확인]

작업:
- 이번에 구현할 기능

확인한 명세:
- 관련 문서명
- 관련 문서명

현재 코드 상태:
- 이미 구현된 부분
- 없는 부분
- 수정이 필요한 부분

구현 계획:
1. ...
2. ...
3. ...

결정 필요:
- 없음

또는

결정 필요:
- 항목
  A. ...
  B. ...
```

단순하고 확정적인 작업에서는 불필요하게 긴 계획을 작성하지 않는다.

---

# 7. 결정이 필요한 경우 답변 형식

문서에 없는 설계 선택이 실제 구현을 막는 경우 임의로 선택하지 않는다.

다음 형식으로 사용자에게 묻는다.

```text
[결정 필요]

항목:
무엇을 결정해야 하는지

현재 명세:
문서에서 확정된 내용

문제가 되는 부분:
왜 현재 상태로 구현 방향을 하나로 정할 수 없는지

선택지:

A. ...
- 장점:
- 단점:
- 기존 명세 변경 여부:

B. ...
- 장점:
- 단점:
- 기존 명세 변경 여부:

구현 관점 권장안:
- A 또는 B
- 이유

사용자 결정 전에는 이 부분을 구현하지 않는다.
```

선택지가 있다고 해서 모든 작업을 중단하지 않는다.

해당 결정과 독립적인 작업은 계속 진행할 수 있다.

---

# 8. 사용자가 "분석만" 요청한 경우

사용자가 다음과 같이 요청하면 코드를 수정하지 않는다.

```text
확인해줘
분석해줘
왜 그런지 봐줘
어떻게 구현할지 알려줘
문제 찾아줘
```

이 경우 repository와 명세를 읽고 원인과 구현 방안만 제시한다.

사용자가 명시적으로 수정 또는 구현을 요청한 경우에만 파일을 변경한다.

---

# 9. 사용자가 구현을 요청한 경우

구현 요청을 받으면 다음 순서를 따른다.

```text
1. 관련 명세 확인
2. 현재 코드 확인
3. 기존 구현 재사용 가능 여부 확인
4. 필요한 최소 변경 범위 결정
5. 구현
6. 빌드
7. 관련 테스트 실행
8. git diff 확인
9. 명세와 결과 대조
10. 사용자에게 변경 결과 보고
```

---

# 10. 구현 완료 후 답변 형식

작업을 완료한 뒤에는 반드시 아래 형식으로 보고한다.

```text
[구현 완료]

구현한 내용:
- ...
- ...

수정한 파일:
- `경로/파일명`
  - 무엇을 변경했는지
- `경로/파일명`
  - 무엇을 변경했는지

명세 반영:
- 어떤 명세 조건을 어떻게 반영했는지
- 어떤 함정을 방지했는지

검증:
- 실행한 명령
- 결과

예:
- `./gradlew test` → 성공
- `./gradlew build` → 성공

남은 사항:
- 없음

또는

남은 사항:
- 아직 팀 결정이 필요한 내용
- 다음 Phase에서 구현할 내용
```

파일을 수정했으면 수정한 파일을 반드시 나열한다.

테스트를 실행하지 못했으면 성공했다고 쓰지 않는다.

대신 다음처럼 명확히 적는다.

```text
검증하지 못한 항목:
- PostgreSQL 실제 연결
- 이유: 로컬 DB 환경변수가 제공되지 않음
```

---

# 11. 오류 수정 답변 형식

버그 수정 요청에서는 다음 형식을 사용한다.

```text
[원인]

증상:
- ...

직접 원인:
- ...

근본 원인:
- ...

[수정]

수정 파일:
- ...

수정 내용:
- ...

[검증]

재현 전:
- ...

수정 후:
- ...

실행한 테스트:
- ...
```

증상만 가리고 근본 원인을 남겨두는 임시 패치를 우선하지 않는다.

---

# 12. DB 변경 규칙

DB 스키마 변경은 Flyway로만 수행한다.

기본 설정:

```yaml
spring:
  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
```

이미 적용된 migration을 수정하지 않는다.

잘못된 스키마를 수정해야 하면 새 migration을 추가한다.

예:

```text
V1__create_core_schema.sql
V2__seed_ports.sql
V3__seed_routes.sql
V4__add_route_path.sql
```

다음 파일을 Flyway와 동시에 DB 초기화 목적으로 사용하지 않는다.

```text
schema.sql
data.sql
```

---

# 13. DB 구현 우선순위

기본적으로 다음 순서를 따른다.

```text
1. PostgreSQL / Flyway 연결
2. ports
3. routes
4. hazard_zones
5. fx_rates
6. eua_prices
7. route_distances
8. forecast / risk data
9. vessel_schedules
10. freight_index_rates
```

후순위:

```text
saved_queries
regulation_chunks
chat_messages
users
sessions
price_alerts
```

사용자가 특정 기능을 먼저 요청한 경우에는 요청을 우선하되 관련 선행조건을 확인한다.

---

# 14. 최신 데이터 기준

항구 데이터는 최신 `08_데이터시드.md`를 기준으로 한다.

```text
출발 항구 18
도착 항구 32
총 50개
```

구버전의 44개 항구 목록을 사용하지 않는다.

항로 상태, 상태 사유, 운임, 거리 등의 기준값도 최신 데이터시드와 계산명세를 우선한다.

---

# 15. EUA API 규칙

현재 EUA 데이터 소스:

```text
Yahoo Finance
CO2.L
```

CO2.L은 EUA 거래소 원장 직접값이 아니라 프록시이다.

이를 실제 EUA 거래소 직접 데이터라고 표현하지 않는다.

기본 캐시:

```text
30분
```

처리:

```text
유효 cache
→ cache 반환

cache 없음
→ 외부 API 호출

호출 성공
→ 저장 + 반환

호출 실패
→ stale snapshot

stale도 없음
→ 명세의 fallback
```

현재가와 전일 대비 변화율은 서로 다른 소스에서 섞지 않는다.

---

# 16. FX API 규칙

현재 환율 데이터 소스:

```text
open.er-api.com
```

구버전의 Frankfurter 기준 구현을 새로 만들지 않는다.

응답에 다음 출처 필드를 유지한다.

```text
source
attribution
attributionUrl
```

환율 계산은 전체 정밀도를 사용한다.

표시 단계 이전에 임의 반올림하지 않는다.

---

# 17. 해상거리 규칙

실제 해상항로 거리와 지도 path는 같은 routing 결과에서 만들어야 한다.

다음을 해상거리로 사용하지 않는다.

```text
Haversine
단순 직선거리
손으로 만든 임의 거리
```

희망봉 항로 restriction에는 Panama 누락 여부를 반드시 확인한다.

거리 계산과 지도 path가 서로 다른 경로를 사용하지 않는다.

`searoute-ts` 처리 방식과 path 저장 위치는 확정되지 않은 경우 사용자에게 먼저 확인한다.

---

# 18. 계산 규칙

계산 공식은 `02_계산명세.md`를 그대로 따른다.

특히 다음을 주의한다.

```text
EEA 역외 ↔ EEA 역내 ETS scope = 0.5
```

2026년 단계적 도입률 100%와 ETS scope 0.5를 혼동하지 않는다.

중간 계산값을 반올림하지 않는다.

총비용은 기본적으로 다음 구조를 따른다.

```text
기본 운임
+ 탄소비용
+ 전쟁위험보험료
(+ CIF일 경우 적하보험 관련 귀속 처리)
```

운항 불가 항로는 총비용을 계산하거나 표시하지 않는다.

접힌 상태뿐 아니라 상세/리포트/AI 응답에서도 동일하게 처리한다.

---

# 19. 납기 계산 규칙

납기는 항구 도착일이 아니라 창고 입고 기한이다.

해상 리드타임 이후 다음 4일을 누락하지 않는다.

```text
수입통관 2일
+
내륙운송 2일
=
4일
```

최대 선적 가능일 계산에서도 동일한 기준을 사용한다.

---

# 20. LLM 규칙

LLM 호출은 서버에서만 한다.

API Key를 frontend에 넣지 않는다.

LLM이 새 숫자를 만들게 하지 않는다.

`compare-routes`에서는 frontend/application 계산 결과 snapshot을 그대로 설명하도록 한다.

다음을 LLM에서 재계산하지 않는다.

```text
totalUsd
baseFreightUsd
carbonCostUsd
warRiskUsd
leadTimeDays
meetsDeadline
확률
거리
환율
EUA 가격
```

LLM Gateway는 HTTP 200이어도 body 안에 오류 코드가 있을 수 있으므로 HTTP status만 확인하지 않는다.

---

# 21. `/api/chat` 규칙

지원 bot:

```text
auto
nav
trip
risk
term
reg
doc
```

주요 규칙:

```text
trip
→ 입력에 없는 값을 임의 기본값으로 채우지 않는다.

term
→ 이전 대화 context를 붙이지 않는다.

risk
→ LLM은 parameter extraction만 한다.
→ 숫자는 risk engine이 계산한다.

doc
→ 화면에 보여준 결과와 다운로드/생성 파일의 내용이 같아야 한다.
```

---

# 22. RAG 규칙

RAG는 실제 구현 범위가 확정된 경우에만 추가한다.

RAG가 미구현인 상태에서 core migration에 pgvector를 강제로 포함하지 않는다.

RAG 구현 시:

```text
조문 단위 chunk
hybrid retrieval
citation 기반 답변
```

이라는 기존 명세를 따른다.

근거에 없는 내용을 생성하지 않는다.

---

# 23. 파일 업로드/텍스트 추출 규칙

지원 범위:

```text
PDF
DOCX
HWPX
TXT
MD
```

최대:

```text
8MB
```

원본 파일을 서버에 영구 저장하지 않는다.

구형 `.hwp`는 지원하지 않는다고 명시한다.

스캔 PDF처럼 텍스트 레이어가 없으면 OCR을 임의 추가하지 않고 해당 상태를 사용자에게 알린다.

---

# 24. 코드 구조

기능 단위 package 구조를 우선한다.

예:

```text
common
config

port
route

market/
  eua/
  fx/

forecast
risk
schedule
freight
ai

regulation
query
auth
alert
```

각 기능 아래 필요한 경우:

```text
controller
service
repository
entity
dto
client
```

를 둔다.

프로젝트 전체를 거대한 전역 `controller/`, `service/`, `repository/` 세 폴더로만 구성하지 않는다.

단, 이미 repository에 팀이 확정한 다른 구조가 있다면 이를 임의로 전면 변경하지 않는다.

---

# 25. DTO / Entity 규칙

JPA Entity를 API Response로 직접 반환하지 않는다.

```text
Entity
→ Service
→ Response DTO
→ Controller
```

Request/Response DTO와 Entity를 분리한다.

금액과 환율 등 정밀도가 필요한 값은 `BigDecimal`을 우선한다.

---

# 26. 외부 API 장애 규칙

외부 API 하나가 실패했다고 전체 서비스가 중단되어서는 안 된다.

반드시 다음 케이스를 고려한다.

```text
cache hit
cache miss + API success
API failure + stale cache
API failure + no stale cache
```

LLM 장애 시에도 계산 기능은 동작해야 한다.

환율/EUA 장애 시 명세에 정의된 fallback 정책을 적용한다.

---

# 27. 테스트 규칙

작업과 직접 관련된 테스트를 실행한다.

가능하면 다음 순서로 확인한다.

```text
단위 테스트
→ 관련 통합 테스트
→ ./gradlew test
→ ./gradlew build
```

DB가 필요한 테스트라면 로컬 DB 또는 테스트 환경이 준비되어 있는지 먼저 확인한다.

환경이 없어 실행하지 못한 테스트는 명확하게 보고한다.

테스트 실패를 숨기거나 통과했다고 표현하지 않는다.

---

# 28. Git 작업 규칙

사용자가 명시적으로 요청하지 않은 경우 다음 작업을 자동으로 하지 않는다.

```text
git commit
git push
branch 삭제
force push
reset --hard
```

코드 수정 후 `git diff`와 `git status`를 확인한다.

사용자가 커밋을 요청하면 변경 범위를 다시 확인한 뒤 수행한다.

---

# 29. 불필요한 변경 방지

한 기능을 구현하면서 관련 없는 파일까지 정리하지 않는다.

예:

```text
API 하나 구현
→ 프로젝트 전체 formatting 금지

버그 하나 수정
→ unrelated package rename 금지

DB 컬럼 하나 추가
→ 전체 Entity 구조 재작성 금지
```

필요한 최소 변경을 우선한다.

---

# 30. 답변에서 반드시 구분할 것

답변에는 다음 세 종류를 혼합하지 않는다.

### 명세에 확정된 내용

```text
[명세]
```

### 현재 코드에서 확인한 내용

```text
[현재 구현]
```

### Claude가 제안하는 내용

```text
[제안]
```

특히 제안을 마치 팀에서 확정된 설계인 것처럼 표현하지 않는다.

---

# 31. 구현 중 명세와 코드가 충돌하는 경우

다음 순서로 처리한다.

```text
1. 현재 코드가 왜 다른지 확인
2. 관련 최신 명세 확인
3. 의도된 변경인지 판단 가능한 근거 확인
4. 근거가 없으면 사용자에게 질문
```

명세가 최신이라는 이유만으로 기존 코드를 대규모로 덮어쓰지 않는다.

현재 구현이 팀의 후속 결정일 가능성을 확인한다.

---

# 32. 작업 중 발견한 문제 보고

요청한 작업과 직접 관련된 문제는 같이 보고한다.

하지만 요청 범위 밖의 문제를 발견했다고 임의로 전부 수정하지 않는다.

형식:

```text
[추가 발견]

- 문제:
- 영향:
- 현재 작업과의 관련성:
- 수정 필요 여부:
```

긴급하지 않으면 사용자 확인 후 별도 작업으로 처리한다.

---

# 33. Claude가 작업을 끝냈다고 판단하는 기준

다음 조건을 모두 확인한 뒤 완료로 보고한다.

```text
[ ] 요청 기능 구현
[ ] 관련 명세 위반 없음
[ ] 불필요한 범위 변경 없음
[ ] 컴파일 오류 없음
[ ] 관련 테스트 확인
[ ] git diff 확인
[ ] 변경 파일 목록 정리
[ ] 남은 결정 사항 구분
```

---

# 34. 최종 응답 기본 템플릿

일반 구현 완료 응답은 아래 형태를 기본으로 한다.

```text
[구현 완료]

구현:
- ...

수정 파일:
- `...`
- `...`

핵심 처리:
- ...

명세 확인:
- `02_계산명세.md`
- `03_API계약.md`
- ...

검증:
- `./gradlew test` → 성공
- `./gradlew build` → 성공

추가 확인 필요:
- 없음
```

결정이 남아 있으면:

```text
추가 확인 필요:
- `route_distances.path` 저장 방식은 아직 미확정
- 현재 구현에서는 해당 부분을 건드리지 않음
```

---

# 35. 사용자와의 작업 방식

사용자는 이 프로젝트의 백엔드 구현을 담당한다.

따라서 답변은 단순히 "완료했습니다"라고 끝내지 않고 다음을 알 수 있게 작성한다.

```text
무엇을 구현했는지
왜 그렇게 구현했는지
어떤 파일이 바뀌었는지
명세의 어느 부분을 따른 것인지
무엇을 검증했는지
무엇이 아직 결정되지 않았는지
```

다만 이미 확정된 단순 작업에서 과도한 설명으로 진행을 방해하지 않는다.

사용자가 직접 판단해야 하는 설계 결정은 숨기지 않는다.

---

# 36. 가장 중요한 마지막 규칙

**명세에 있는 것을 정확하게 구현한다.**

**명세에 없는 것을 필요 이상으로 만들지 않는다.**

**결정되지 않은 것을 임의로 결정하지 않는다.**

**숫자를 LLM에게 맡기지 않는다.**

**수정 후에는 실제 테스트 결과를 확인한다.**
