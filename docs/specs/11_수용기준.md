# 수용 기준 — 개발 지시서로 그대로 써도 되는 형태

각 항목은 **누가 봐도 통과/실패가 갈리게** 썼다. 개발자에게 지시할 때 이 표를 그대로
잘라 주면 된다. 애매한 표현("잘 보이게", "적절히")은 일부러 뺐다.

**기준 조건** — 달리 적지 않으면 전부 이 조건이다:
부산항 → 함부르크항 · 컨테이너 20ft · FOB · 납기 2026-10-05 · 오늘 2026-07-24 ·
EUA €79.01 · 1 EUR = 1.1548 USD

---

## F-1. 총비용 3분해 계산

| # | 기준 | 통과 조건 |
|---|---|---|
| 1.1 | 탄소비용 | C 항로 **$86**(±$1). 적용범위 0.5 미적용 시 $172 가 나온다 — 그러면 실패 |
| 1.2 | 전쟁위험보험료 | A **$9** · D **$54** · C **$0** · B $68 |
| 1.3 | 총비용 | A **$2,916** · D **$3,021** · C **$3,286** |
| 1.4 | 정렬 | 총비용 오름차순, **운항 불가는 맨 뒤** |
| 1.5 | 40ft 전환 | 운임 ×1.8, 탄소·보험 ×2 로 각각 다르게 반영된다 |
| 1.6 | 완성차 100대 | 컨테이너 20ft 와 **총비용이 같으면 실패** ([함정 2](06_함정목록.md)) |
| 1.7 | 도착지 영국 | 탄소비용 **$0** (EU ETS 미적용) |

## F-2. 상태와 추천

| # | 기준 | 통과 조건 |
|---|---|---|
| 2.1 | 추천 항로 | **C 희망봉**. A(최저가)가 추천되면 실패 |
| 2.2 | 배지 | A·D `제한적` · B `운항 불가` · C `추천` |
| 2.3 | 배지 툴팁 | 마우스 올리면 `statusReason` 전문이 뜬다 |
| 2.4 | 운항 불가 — 접힘 | 총비용 자리에 `운항 불가`, 비용 구성 자리에 `운항 불가 구간이라 비용을 산출하지 않습니다` |
| 2.5 | 운항 불가 — **펼침** | **금액·최대 선적일이 하나도 없다.** 사유와 참고 거리만 |
| 2.6 | 운항 불가 — 비교 | 체크박스 비활성 + 툴팁 |

> 2.5 는 **접힘만 확인하고 넘어가면 안 된다.** 반드시 펼쳐서 본다.

## F-3. 인코텀즈 귀속

| # | 기준 | 통과 조건 |
|---|---|---|
| 3.1 | FOB (C) | 매수인 **$3,286** / 매도인 **$0** |
| 3.2 | CIF (C, 가액 $50,000) | 매수인 **$0** / 매도인 **$3,374** / 적하보험료 **$88** |
| 3.3 | CIF, 가액 없음 | 보험료 자리에 `화물 가액 입력 필요`. **임의 값이 들어가면 실패** |
| 3.4 | 전환 시 레이아웃 | CIF 로 바꿔도 조건 바가 깨지지 않는다 |
| 3.5 | 용어 | 화면 어디에도 비용 귀속 설명에 **"화주"** 가 없다 |
| 3.6 | 총비용 불변 | FOB↔CIF 를 바꿔도 **총비용(누가 내든 합계)은 그대로** — 배율이 곱해지면 실패 |

## F-4. 납기 역산

| # | 기준 | 통과 조건 |
|---|---|---|
| 4.1 | 최대 선적 가능일 | **2026-08-10** (= 납기 − (52+4)) |
| 4.2 | 문전-문전 | **65일** (육상 13 + 해상 52) |
| 4.3 | 두 화면 일치 | 경로 추천 탭과 공급망 일정 탭이 **같은 날짜**를 말한다 |
| 4.4 | 여유 표기 | `✓ 납기 충족 · 17일 여유` 처럼 **남은 날짜까지** 나온다 |
| 4.5 | 영업일 | 영업일 표기는 주말을 실제로 세어 계산한다. `달력일 × 5/7` 근사 금지 |

## F-5. 항로 거리와 지도

| # | 기준 | 통과 조건 |
|---|---|---|
| 5.1 | 거리 | A·B **11,259** · D **11,317** · C **14,549** nm |
| 5.2 | 희망봉 경로 | 지도의 C 선이 **아프리카 남단**을 지난다. 태평양·파나마로 가면 실패 |
| 5.3 | 선과 거리 일치 | 지도 선이 거리 계산이 돌려준 좌표다 |
| 5.4 | 경도 | 선이 지도를 가로지르지 않는다 |
| 5.5 | 폴백 | 거리 계산이 실패한 항로만 개별 폴백된다(전부 아니면 전무 금지) |

## F-6. 리스크 엔진

| # | 기준 | 통과 조건 |
|---|---|---|
| 6.1 | 구간 | 14영업일 80% 구간이 현재가를 **감싼다**(lo80 < spot < hi80) |
| 6.2 | 중앙선 | 드리프트 0 일 때 중앙선 = 현재가 |
| 6.3 | 도달 확률 | 목표가 ≥ 현재가면 **1**. 목표가가 낮을수록 확률이 **단조 감소** |
| 6.4 | 이산감시 보정 | 보정을 빼면 확률이 눈에 띄게 커진다 — 넣은 쪽이 맞다 |
| 6.5 | 극단 입력 | 외부 예측에 −100% 이하를 넣어도 **NaN 이 화면에 안 나온다** |
| 6.6 | 선대 리스크 | 3개월 기준 **$296.4M** · 연료비/탄소비용 **11.6배** |
| 6.7 | 지평 표기 | 3개월/1년 토글이 있고, 화면이 **어느 지평인지 말한다** |
| 6.8 | 변동성 고정 | 실시세를 받아도 변동성이 다시 계산되지 않는다 |

## F-7. 실시간 연동

| # | 기준 | 통과 조건 |
|---|---|---|
| 7.1 | EUA | 헤더 가격이 API 값과 일치 |
| 7.2 | 등락률 | 헤더 등락률이 **같은 응답의 값**과 일치 ([함정 6](06_함정목록.md)) |
| 7.3 | 환율 | 각주가 `실시간 환율` 로 표시. 폴백이면 그렇게 표시 |
| 7.4 | 출처 표기 | 환율 출처 문구가 화면에 있다 |
| 7.5 | 장애 내성 | API 를 전부 막아도 **화면이 뜨고 계산이 된다** |
| 7.6 | 각주 갱신 | 실시세 도착 후 각주 숫자가 **함께 바뀐다**(옛 값이 굳으면 실패) |

## F-8. AI (붙였다면)

| # | 기준 | 통과 조건 |
|---|---|---|
| 8.1 | 숫자 일치 | AI 답변의 금액이 화면 금액과 **같다** |
| 8.2 | 없는 값 | 근거에 없으면 `제공된 조문에서는 확인되지 않습니다` |
| 8.3 | 재현성 | 같은 규정 질문 3회 → **같은 조문** 인용 |
| 8.4 | 없는 항만 | 목록에 없는 항만은 `unsupported` 로 표시, 조건에 안 들어간다 |
| 8.5 | 기본값 금지 | 문장에 없는 항목을 임의 기본값으로 채우지 않는다 |
| 8.6 | 상태 사유 인용 | 항로 비교 리포트가 `제한적` 항로의 **사유를 근거로** 말한다 |
| 8.7 | 경로 배지 | 답변마다 엔진/색인/생성 중 무엇으로 답했는지 표시 |
| 8.8 | 키 없음 | LLM 키가 없어도 **계산 기능은 전부 동작** |

## F-9. 문서·파일 (T3)

| # | 기준 | 통과 조건 |
|---|---|---|
| 9.1 | 지원 형식 | PDF · DOCX · HWPX · TXT 에서 텍스트가 나온다 |
| 9.2 | `.hwp` | **거부하고 사유를 답한다** |
| 9.3 | 스캔본 PDF | "이미지 PDF 로 보인다"고 답한다 |
| 9.4 | 저장 안 함 | 원본이 서버에 남지 않는다 |
| 9.5 | 다운로드 | 파일 내용이 **화면에 보인 것과 같다**(재생성 금지) |

## F-10. 각주·정직성 — **점수가 여기서 갈린다**

| # | 기준 | 통과 조건 |
|---|---|---|
| 10.1 | 가정값 표기 | 정시운항율·리드타임·보험 요율에 "가정값" 표기 |
| 10.2 | 자체 추정 | 콜롬보 0.6% 가 자체 추정이라고 밝혀져 있다 |
| 10.3 | 프록시 고지 | EUA·벙커유 모두 프록시라고 밝힌다(한쪽만 밝히면 실패) |
| 10.4 | 추정/검증 구분 | 260거래일(추정 창)과 1,728일(검증 표본)이 구분돼 있다 |
| 10.5 | 범위 제외 | 북극항로를 뺀 사유가 화면에 있다 |
| 10.6 | 예측 부인 | 중앙선이 "점 예측이 아니다"라고 명시 |
| 10.7 | ECE 공개 | 보정오차가 기준선보다 나쁘다는 것을 **숨기지 않는다** |

## F-11. 견고성

| # | 기준 | 통과 조건 |
|---|---|---|
| 11.1 | 콘솔 | 전 탭 순회 후 콘솔 에러 **0** |
| 11.2 | 네트워크 | 실패 요청 **0** |
| 11.3 | 탭 경합 | 탭 5개를 빠르게 연속 클릭해도 안 터진다 |
| 11.4 | 반응형 | 1440 / 1920 에서 가로 스크롤이 없다 |
| 11.5 | 서브경로 | 게이트웨이 경로(`/<slug>/`)에서 자산이 깨지지 않는다 |

---

## 개발 지시 예시 — 이렇게 잘라 주면 된다

> **작업**: 항로 비용 비교표
> **입력**: [컴포넌트명세 §2](10_컴포넌트명세.md)의 결과 객체 배열
> **문구**: [문구집 §2·§3·§6](09_문구집.md) 원문 그대로
> **생김새**: [화면명세 §5](04_화면명세.md) + Figma 핸드오프 캔버스 `68:1735`
> **완료 조건**: 수용기준 **F-1.3 / 1.4 / F-2.2 / 2.4 / 2.5 / 2.6** 전부 통과
> **주의**: [함정 3](06_함정목록.md) — 접힘·펼침 **양쪽** 모두 금액을 감춰야 한다

지시 하나에 **입력 · 문구 · 생김새 · 완료 조건 · 알려진 함정** 다섯 개가 다 들어가야
같은 결과가 나온다. 하나라도 빠지면 사람마다 다른 걸 만든다.
